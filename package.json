{
  "name": "direction-design",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "generate": "node scripts/generate-project-shells.mjs",
    "test": "node --test tests/*.test.mjs",
    "check:reset": "node --test tests/*.test.mjs",
    "build": "node scripts/generate-project-shells.mjs && node --test tests/*.test.mjs"
  }
}
